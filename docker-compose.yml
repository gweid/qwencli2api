version: '3.8'

services:
  qwen-api:
    build: .
    image: ghcr.io/water008/qwenapi:latest
    ports:
      - "3008:3008"
    volumes:
      - ./data:/app/data
    env_file:
      - .env
    restart: unless-stopped
    healthcheck:
      test: ["CMD", "curl", "-f", "http://localhost:3008/api/health"]
      interval: 600s
      timeout: 10s
      retries: 3
      start_period: 40s
